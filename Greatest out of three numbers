#include <stdio.h>
int main()
{
  int a,b,c;
  printf("enter the two numbers");
  scanf("%d%d%d",&a,&b,&c);
  if(a>b && a>c)
  {
    printf("A greatest");
  }
  else if(b>a && b>c)
  {
    printf("B greatest");
  }
  else
  {
    printf("C greatest");
  }
  
}
  
